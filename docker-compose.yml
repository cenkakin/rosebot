services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: rosebot
      POSTGRES_USER: rosebot
      POSTGRES_PASSWORD: rosebot
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
